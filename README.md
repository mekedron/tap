# Homebrew Tap

Install this tap:

```bash
brew tap mekedron/tap https://github.com/mekedron/tap
```

Install `wolt-cli`:

```bash
brew install mekedron/tap/wolt-cli
```

Verify:

```bash
wolt --help
```
